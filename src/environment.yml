name: be
channels:
  - conda-forge
dependencies:
  # Core Python version
  - python=3.12
  - pip
  
  # Conda dependencies
  - numpy
  - xarray
  - scipy
  - matplotlib
  - netcdf4
  - xrft=0.3.2
  - pandas
  - earthaccess
  - h5py
  - pyproj
  - cmocean
  - cartopy
  - ipython
  - pyjuliacall
  - jupyterlab
  - notebook
  - dask
  
  # Build the module with pip
  - pip:
    - -e .
